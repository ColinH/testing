%include "example.config3"

foo.bar.baz = true

foo.bar.fibble = false

pond.frog = {
   foo.bar.baz = null
}

fobble = [ true, true, false ]
fibble = [ true true false ]

array = [ [
  { a : true, b : false }
] ]

include.file = {
   %include "example.config3"
}

four = null

fibble.1 = null
fibble.- = null

copied = {
   again = %copy include
}

copied.subtree = %copy pond.frog

copied.array_test = %copy array.0

number = 42

reference = (pond)

zzz = 1 + 2 + 3